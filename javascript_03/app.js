const result = square(5);
console.log('Square of 5 is ' + result);

const squareResult = addSquare(10, 20);
console.log('Sum of 10 and 20 square is ' + squareResult);

const num = 5;
const factorailResult = factorial(Number(num));
console.log('Factorial of ' + num + ' is ' + factorailResult);
